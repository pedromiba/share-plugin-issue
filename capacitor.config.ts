import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.share.plugin.issue',
  appName: 'share-plugin-reproduction-issue',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
